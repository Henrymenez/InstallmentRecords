namespace InstallmentRecords
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Application app = new Application();
            app.Run();

            /*TODO LIST
             * Create a Customer class, should take in the customer name, product name, product amount
             * Each installment plan should be a method in the instalment records class that takes in a  customer class as paramter
             * each installment will have a diffrent % rate to calculate the total payment plan
             * the run method will collect the customer name, product name, product amount, the pick installment plan
             * create an instance list of the Customer class and pass it into the plan
             *      ADD MORE STUFFS     
             * 
             * 
             */
        }
    }
}